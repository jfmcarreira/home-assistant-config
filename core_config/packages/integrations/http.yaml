################################################################################
## HTTP
################################################################################
http:
  base_url: !secret server_address
  server_port: !secret server_port
  trusted_proxies:
    - 127.0.0.1
    - ::1
  ip_ban_enabled: true
  login_attempts_threshold: 10
  use_x_forwarded_for: true
#  ssl_certificate: /ssl/fullchain.pem
#  ssl_key: /ssl/privkey.pem
