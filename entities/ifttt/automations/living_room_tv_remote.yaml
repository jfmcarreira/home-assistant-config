# This handles the remote control LG TV
#
# https://www.home-assistant.io/integrations/ifttt/
#
# Body configured in IFTTT: { "action": "remote_control_tv", "button":"{{TextField}}" }

id: IFTTT LG TV Remote
alias: IFTTT LG TV Remote
trigger:
  - event_data:
      action: remote_control_tv
    event_type: ifttt_webhook_received
    platform: event
action:
  - data_template:
      button: "{{ trigger.event.data.button }}"
      entity_id: media_player.living_room_tv
    service: python_script.living_room_tv_remote