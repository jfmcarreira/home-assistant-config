platform: history_stats
name: Fridge Door Open Time Daily
entity_id: binary_sensor.fridge_door
state: "on"
type: time
start: "{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}"
end: "{{ now() }}"
