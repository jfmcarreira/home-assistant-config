wifi:
  domain: .home
  ssid: !secret esp_ssid
  password: !secret esp_password
  #use_address: 10.20.107.137
  power_save_mode: "${PowerSaveMode}"

logger:

ota:
  reboot_timeout: 0s

web_server:

captive_portal:
