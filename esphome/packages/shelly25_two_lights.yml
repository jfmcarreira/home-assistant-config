packages:
  device_lights: !include shelly25_lights.yml

light:
  - id: light_1
    name: ${Light_1_Name}
    platform: binary
    output: relay_1
    restore_mode: RESTORE_DEFAULT_OFF

  - id: light_2
    name: ${Light_2_Name}
    platform: binary
    output: relay_2
    restore_mode: RESTORE_DEFAULT_OFF
