# This handles the loading of IFTTT automations
#
# https://www.home-assistant.io/integrations/shell_command/
#

# The MEO box IP is stored in a helper entity
shell_command:
  meo_box_reset_program: 'python3 /config/scripts/meo_box_reset_program.py {{ states("input_text.helper_living_room_tv_meo_box_ip") }}'
