################################################################
## Zoneminder
################################################################
zoneminder:
  - host: !secret zm_host
    ssl: false
    #username: !secret zm_user
    #password: !secret zm_pass
sensor:
  - platform: zoneminder
    include_archived: false
switch:
  - platform: zoneminder
    command_on: Modect
    command_off: Monitor
camera:
  - platform: zoneminder
