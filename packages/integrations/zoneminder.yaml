################################################################
## Zoneminder
################################################################
zoneminder:
  - host: !secret zm_host
    ssl: true
    verify_ssl: false
    #username: !secret zm_user
    #password: !secret zm_pass

sensor:
  - platform: zoneminder
    include_archived: false

#switch:
  #- platform: zoneminder
    #command_on: Modect
    #command_off: Monitor

camera:
  - platform: zoneminder

#shell_command:
  #zoneminder_ptz: /usr/bin/curl -X POST "http://zoneminder.home/zm/index.php?view=request" -d "request=control&id=2&control=moveCon{{ direction }}"
