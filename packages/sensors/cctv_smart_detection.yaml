template:
- binary_sensor:
    - name: Backyard Camera Smart Detection
      unique_id: backyard_camera_smart_detection
      device_class: motion
      icon: mdi:cctv
      availability: >-
        {{
            (states('binary_sensor.backyard_camera_reolink_person') not in ('unknown', 'unavailable')) and
            (states('binary_sensor.backyard_camera_reolink_vehicle') not in ('unknown', 'unavailable')) and
            (states('binary_sensor.backyard_camera_person') not in ('unknown', 'unavailable')) and
            (states('binary_sensor.backyard_camera_car') not in ('unknown', 'unavailable'))
        }}
      state: >
        {{  is_state('binary_sensor.backyard_camera_reolink_person', 'on') or
            is_state('binary_sensor.backyard_camera_reolink_vehicle', 'on') or
            is_state('binary_sensor.backyard_camera_person', 'on') or
            is_state('binary_sensor.backyard_camera_car', 'on')
        }}

    - name: Front Camera Smart Detection
      unique_id: front_camera_smart_detection
      device_class: motion
      icon: mdi:cctv
      availability: >-
        {{
            (states('binary_sensor.front_camera_reolink_person') not in ('unknown', 'unavailable')) and
            (states('binary_sensor.front_camera_reolink_vehicle') not in ('unknown', 'unavailable')) and
            (states('binary_sensor.front_camera_person') not in ('unknown', 'unavailable')) and
            (states('binary_sensor.front_camera_car') not in ('unknown', 'unavailable'))
        }}
      state: >
        {{  is_state('binary_sensor.front_camera_reolink_person', 'on') or
            is_state('binary_sensor.front_camera_reolink_vehicle', 'on') or
            is_state('binary_sensor.front_camera_person', 'on') or
            is_state('binary_sensor.front_camera_car', 'on')
        }}
