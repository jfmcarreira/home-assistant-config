template:
  - sensor:
      - unique_id: grocy_stock_water_filters
        name: grocy_stock_water_filters
        state: >
          {% set products = state_attr('sensor.grocy_stock', 'products') %}
          {% if products %}
            {% set product = products | selectattr('id', 'eq', 99) | list | first %}
            {{ product.available_amount if product is not none else 0 }}
          {% else %}
            0
          {% endif %}
