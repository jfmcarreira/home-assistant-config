template:
  - sensor:
      - unique_id: grocy_stock_water_filters
        name: grocy_stock_water_filters
        state: >
          {% set products = state_attr('sensor.grocy_stock', 'products') %}
          {% set matches = products | selectattr('id', 'eq', 99) | list %}
          {{ matches[0].available_amount if matches | length > 0 else 0 }}
